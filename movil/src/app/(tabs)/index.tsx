import { Pantalla, Proximamente } from "@/components/Pantalla";

export default function Inicio() {
  return (
    <Pantalla titulo="Velvet Wallet">
      <Proximamente que="Disponible del mes, ingresos y gastos, saldo proyectado. Requiere sueldo configurado y clasificación." />
    </Pantalla>
  );
}
